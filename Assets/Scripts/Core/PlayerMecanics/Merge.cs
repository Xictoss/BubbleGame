using System.Globalization;
using Unity.VisualScripting.YamlDotNet.Core.Tokens;
using UnityEditor;
using UnityEngine;

namespace BubbleGame.Core
{
    public class Merge : MonoBehaviour
    {
        [SerializeField]
        private void Start()
        {
            //if (CompareInfo.GetCompareInfo(Id = )) ;
        }
    }   
        
}    
